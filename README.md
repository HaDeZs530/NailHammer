# Nailhammer

A phone-first game (Vite + React + TypeScript now; iOS wrap later). Concept: `// DESIGN-OPEN:` see `docs/Nailhammer_Design_Doc.md` §1 once the Design Chat writes it.

**Agents: read [`CLAUDE.md`](CLAUDE.md) first.** Design source of truth and project state live in [`docs/`](docs/). Process is copied from the Eldrathor `PROCESS_TEMPLATE`.
